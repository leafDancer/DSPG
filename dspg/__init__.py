"""DSPG: distribution-based structural policy gradient — experiment code.

Import submodules as ``dspg.pe_dspg``, ``dspg.pe_vfi``, etc.; run CLI entry points with
``python -m dspg.<module>`` from the repository root (see README).
"""
